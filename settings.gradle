rootProject.name = 'simpletron-core'

include 'impl'
include 'contract'
include 'component'
rootProject.children.forEach { project ->
    project.projectDir = new File(settingsDir, "core\\$project.name")
}

if (!System.env.JITPACK) {
    include 'cmd_app'
}